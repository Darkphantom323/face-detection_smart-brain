{
  "name": "smart-brain",
  "version": "1.0.0",
  "dependencies": {
    "concurrently": "^6.2.2"
  },
  "scripts": {
    "start": "concurrently \"cd smart-brain-master(Front-end) && npm install && npm start\" \"cd smart-brain-boost-api-master(Back-end) && npm install && npm start\""
  }
}
